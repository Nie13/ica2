# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
production:
  adapter: mysql2
  database: cityspade_development
  hostname: localhost
  user: root
  password:

test:
  adapter: mysql2
  database: cityspade_test
  host: localhost
  username:
  password:

development:
  adapter: mysql2
  database: cityspade_development
  host: localhost
  username:
  password:
  encoding: utf8mb4
  collation: utf8mb4_unicode_ci

staging:
  adapter: mysql2
  database: cityspade_staging
  hostname: localhost
  user: root
  password:


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
